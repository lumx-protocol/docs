---
title: "Create a project"
description: "Discover what you can achieve with our Protocol in a few steps"
---

<Snippet file="admin-authentication.mdx" />

#### Step 1. Add your API key to the request headers.

<CodeGroup>

```bash cURL
API_KEY=API_KEY
```

```javascript JavaScript
import axios from "axios";

axios.defaults.headers.common["Authorization"] = API_KEY;
```

</CodeGroup>

We are using [axios](https://axios-http.com/) in this example, but you can use
whatever library you prefer.

#### Step 2. Let's insert the test endpoint.

You can use this endpoint to check if your API key is working.

<CodeGroup>

```bash cURL
curl --request POST \
--url https://protocol-sandbox.lumx.io/v1/wagmi \
--header 'Bearer: $API_KEY'
```

```javascript JavaScript
axios.get("https://protocol.lumx.io/v1/wagmi").then((response) => {
  // Handle Success
  console.log(response);
});
```

</CodeGroup>

You will receive a status 200 with the message below.

```bash
"Congratulations <CLIENT_NAME>! Let's start our journey together."
```

If you completed all the steps above, you can advance to our next recipe.

<Snippet file="troubleshooting.mdx" />
